{% assign paginator = include.paginator %}

{% if paginator.previous or paginator.next %}
<nav class="paginator" aria-label="Paginacja">
	<ol class="paginator-list">
	{% if paginator.previous %}
	<li class="paginator-item">
		<a href="{{ paginator.previous | replace: '//', '/' }}" class="paginator-link" rel="nofollow"><span aria-hidden="true">←</span> Nowsze wpisy</a>
	</li>
	{% endif %}

	{% if paginator.next %}
	<li class="paginator-item">
		<a href="{{ paginator.next | replace: '//', '/' }}" class="paginator-link" rel="nofollow">Starsze wpisy <span aria-hidden="true">→</span></a>
	</li>
	{% endif %}
	</ol>
</nav>
{% endif %}
